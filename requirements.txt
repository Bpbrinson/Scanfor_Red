# Python dependencies for scanfor_red.
# Note: opencv-python-headless (no GUI libs) is used because the app never
# opens windows -- it only reads, processes, and writes image/JSON files.
# This is the right choice for containers and servers.
opencv-python-headless>=4.9
numpy>=1.26
pytesseract>=0.3.13
openpyxl>=3.1
# Web frontend (upload screenshots, view result summaries)
Flask>=3.0
# Dashboard screenshot capture (Capture Dashboard & Scan button).
# In 'cdp' mode this drives your already-running Chrome; in 'playwright' mode
# it also needs a browser binary -- run 'playwright install chromium' once
# (the Dockerfile does this for the image).
playwright>=1.40
